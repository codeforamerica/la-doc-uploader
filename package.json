{
  "private": true,
  "scripts": {
    "test": "sudo percy exec -- ./gradlew clean test --tests LaDocUploadFlowJourneyTest.fullUploadDocumentFlow"
  },
  "devDependencies": {
    "@percy/cli": "^1.20.0"
  }
}
